Rendering README Assets
=======================

The PNG figures committed under ``docs/assets/`` are generated by
``viz.render_readme_assets``.  They are produced from the real TrackGen runtime
pipeline with fixed seeds and default geometry parameters, then written to disk
so they can be displayed by GitHub.

How to regenerate
-----------------

Run the module from the repository root:

.. code-block:: bash

   .venv/bin/python -m viz.render_readme_assets

The script prints the path of each file it writes.

What gets written
-----------------

``render_readme_assets()`` regenerates **all** committed figures in one call.
The figures it produces are:

.. list-table::
   :header-rows: 1
   :widths: 35 65

   * - File
     - Contents
   * - ``docs/assets/readme-generator-grid.png``
     - Five representative raw phase-1 centerlines from each of the five
       standard generators (25 panels total), rendered at fixed seeds.
   * - ``docs/assets/readme-pipeline-stages.png``
     - A single Bezier sample shown at four pipeline stages: Phase 1 raw
       centerline, constant-spacing resample, XPBD-relaxed band, and the final
       inflated road.
   * - ``docs/assets/readme-generator-strip.png``
     - One representative phase-1 centerline per standard generator in a
       single horizontal strip.
   * - ``docs/assets/readme-gate-strip.png``
     - The phase-2 gate collision solve illustrated for each generator: raw
       anchors (top row) and separated gates with tangent frames and gate-width
       bars (bottom row).
   * - ``docs/assets/generator-<name>.png`` (five files)
     - Per-generator panel showing five representative raw phase-1 outputs.
       One file per standard generator (``bezier``, ``checkpoint``, ``hull``,
       ``polar``, ``voronoi``).

.. warning::

   ``render_readme_assets()`` **rewrites every figure unconditionally**.  If
   you run it and only one figure was supposed to change, all the others are
   still overwritten (with fresh but deterministic content).  Review ``git
   diff --stat docs/assets/`` before committing, and stage only the files you
   actually intended to update.

Implementation notes
--------------------

The function lives in ``viz/render_readme_assets.py``.  It calls
``TrackGenerator`` on the ``cpu`` device with a small batch (24 environments)
and fixed seeds, so results are deterministic and GPU-free.  Individual helper
functions — ``render_generator_panels()`` and ``render_gate_assets()`` — can be
called independently if only a subset of assets needs to be rebuilt; both are
defined in the same module.
