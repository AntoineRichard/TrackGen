\begin{figure}[t]
  \centering
  \footnotesize
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
    >=Stealth,
    node distance=2mm,
    box/.style={draw=neutralcolor, rounded corners=1pt, align=center,
      minimum height=8mm, text width=14mm, inner sep=1.5pt},
    private/.style={box, dashed, fill=black!5},
    public/.style={box, draw=groundcolor, fill=groundcolor!10},
    future/.style={box, dashed, draw=aircolor, fill=aircolor!10},
    arrow/.style={-{Stealth[length=1.4mm]}, semithick, draw=neutralcolor},
    note/.style={-{Stealth[length=1.1mm]}, dashed, thin, draw=neutralcolor}
  ]
    \node[box] (inputs) {Seeds\\config};
    \node[box, right=of inputs] (phaseone) {Phase~1\\generator};
    \node[box, right=of phaseone] (resample) {Constant-spacing\\resampling};
    \node[box, right=of resample, text width=18mm] (project)
      {XPBD\\projection};
    \node[box, right=of project] (inflate) {Inflation};
    \node[box, right=of inflate] (validity) {Final geometric\\validity};
    \node[public, right=of validity, text width=17mm] (artifacts)
      {Public current\\\texttt{Track}};
    \node[public, below=6mm of artifacts] (utilities) {Runtime\\utilities};
    \node[private, below=7mm of resample, text width=34mm] (private)
      {Internal buffers: raw, pre, post\\No public lineage};
    \node[future, below=7mm of inflate, text width=31mm] (future)
      {Future adapters: simulator/RL, manifests, immutable corpus};

    \draw[arrow] (inputs) -- (phaseone);
    \draw[arrow] (phaseone) -- (resample);
    \draw[arrow] (resample) -- (project);
    \draw[arrow] (project) -- (inflate);
    \draw[arrow] (inflate) -- (validity);
    \draw[arrow] (validity) -- (artifacts);
    \draw[arrow] (artifacts) -- (utilities);
    \draw[note] (phaseone.south) -- (private.north west);
    \draw[note] (project.south) -- (private.north east);
    \draw[note] (artifacts.south west) -- (future.north east);
  \end{tikzpicture}%
  }
  \caption{Current staged runtime pipeline and its release boundary. Dashed gray
  boxes mark internal buffers without public lineage; blue boxes mark public current
  artifacts and utilities; dashed red boxes mark future adapters or release artifacts.
  The separate gate-generation path is described in the text and is not shown.}
  \label{fig:runtime-pipeline}
\end{figure}
