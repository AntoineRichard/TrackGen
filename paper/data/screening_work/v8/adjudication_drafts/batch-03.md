# Adjudication Batch 03

## C0048

Locked ratings:
- `A-C0048-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0048 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0048-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0048; retrieval_notes` and binds `https://docs.unity3d.com/Manual/UnityManual.html`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Unity (C0048) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0053

Locked ratings:
- `A-C0053-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- `A-C0053-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0053 metadata-only row` and binds `https://www.esri.com/en-us/arcgis/products/arcgis-cityengine/overview`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: ArcGIS CityEngine (C0053) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0055

Locked ratings:
- `A-C0055-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0055 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0055-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0055 has no staged full text; metadata-only record cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0055 provisional-metadata row` and binds `https://pmc.ncbi.nlm.nih.gov/articles/PMC21766/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Global Curvature, Thickness, and the Ideal Shapes of Knots (C0055) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0056

Locked ratings:
- `A-C0056-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0056 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0056-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0056 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0056; retrieval_notes` and binds `https://link.springer.com/article/10.1007/s00222-002-0234-y`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: On the minimum ropelength of knots and links (C0056) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0057

Locked ratings:
- `A-C0057-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0057 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0057-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0057 has no staged full text; metadata-only record cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0057 provisional-metadata row` and binds `https://www.cs.cmu.edu/~kmcrane/Projects/RepulsiveCurves/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Repulsive Curves (C0057) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0058

Locked ratings:
- `A-C0058-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0058 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- `A-C0058-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0058 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest record C0058/provisional-metadata; retrieval_notes` and binds `https://ieeexplore.ieee.org/document/10965488/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: From Generation to Gameplay: Authoring Race Tracks With Repulsive Curves (C0058) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0059

Locked ratings:
- `A-C0059-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0059 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0059-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0059 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0059 provisional-metadata row` and binds `https://arxiv.org/abs/2203.16383`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Tangent-point energies and ropelength as Gamma-limits of discrete tangent-point energies on biarc curves (C0059) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0060

Locked ratings:
- `A-C0060-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0060 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0060-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0060 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0060 provisional-metadata row` and binds `https://matthias-research.github.io/pages/publications/posBasedDyn.pdf`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Position Based Dynamics (C0060) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0061

Locked ratings:
- `A-C0061-04` / `screening-04`: **excluded / `exclude-out-of-scope`**. Reason: The paper establishes compliant position-based dynamics for deformable-body simulation but no course representation generation transferable route/test-input contribution supporting property or contextual review.
- `A-C0061-05` / `screening-05`: **excluded / `exclude-out-of-scope`**. Reason: The report defines a position-based dynamics method for deformable-body simulation and provides no course representation generator benchmark or transferable route property.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `full_text` at `PDF p. 1; Abstract and Section 1 Introduction` and binds `https://dl.acm.org/doi/10.1145/2994258.2994272`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: XPBD: Position-Based Simulation of Compliant Constrained Dynamics (C0061) establishes compliant position-based dynamics for deformable-body simulation, but no frozen evidence establishes a course representation, course generator, transferable route/test-input property, or contextual review.

- Recommendation: **excluded / `exclude-out-of-scope`**

## C0062

Locked ratings:
- `A-C0062-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0062 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0062-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0062 provisional-metadata row` and binds `https://ieeexplore.ieee.org/document/10093017/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Robotic Manipulation of Deformable Rope-Like Objects Using Differentiable Compliant Position-Based Dynamics (C0062) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0064

Locked ratings:
- `A-C0064-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0064 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- `A-C0064-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest record C0064/provisional-metadata; retrieval_notes` and binds `https://github.com/NVIDIA/warp`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Warp: A High-performance Python Framework for GPU Simulation and Graphics (C0064) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0065

Locked ratings:
- `A-C0065-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0065 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0065-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0065 has no staged full text; metadata-only record cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0065 provisional-metadata row` and binds `https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: CUDA Graphs (C0065) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0066

Locked ratings:
- `A-C0066-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0066 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0066-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0066 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0066 provisional-metadata row` and binds `https://www.nature.com/articles/s41586-020-03157-9`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: First return, then explore (C0066) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0067

Locked ratings:
- `A-C0067-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0067 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- `A-C0067-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0067 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest record C0067/provisional-metadata; retrieval_notes` and binds `https://ieeexplore.ieee.org/document/4219051/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Towards automatic personalised content creation for racing games (C0067) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0068

Locked ratings:
- `A-C0068-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0068 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0068-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0068; retrieval_notes` and binds `https://ieeexplore.ieee.org/document/5756645/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Search-Based Procedural Content Generation: A Taxonomy and Survey (C0068) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0070

Locked ratings:
- `A-C0070-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0070 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0070-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0070 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0070 provisional-metadata row` and binds `https://dl.acm.org/doi/10.1145/2422956.2422957`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Procedural Content Generation for Games: A Survey (C0070) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0074

Locked ratings:
- `A-C0074-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0074 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0074-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0074 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0074 provisional-metadata row` and binds `https://arxiv.org/abs/1905.09727`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Deep Drone Racing: From Simulation to Reality With Domain Randomization (C0074) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0075

Locked ratings:
- `A-C0075-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0075 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0075-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0075 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0075; retrieval_notes` and binds `https://arxiv.org/abs/2005.12813`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: AlphaPilot: autonomous drone racing (C0075) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0076

Locked ratings:
- `A-C0076-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0076 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0076-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0076 provisional-metadata row` and binds `https://www.nature.com/articles/s41586-023-06419-4`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Champion-level drone racing using deep reinforcement learning (C0076) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0077

Locked ratings:
- `A-C0077-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0077 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0077-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0077; retrieval_notes` and binds `https://arxiv.org/abs/2310.10943`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Reaching the Limit in Autonomous Racing: Optimal Control versus Reinforcement Learning (C0077) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0078

Locked ratings:
- `A-C0078-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0078 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0078-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0078 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0078; retrieval_notes` and binds `https://www.roboticsproceedings.org/rss20/p109.pdf`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: MPCC++: Model Predictive Contouring Control for Time-Optimal Flight with Safety Constraints (C0078) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0079

Locked ratings:
- `A-C0079-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0079 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0079-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0079; retrieval_notes` and binds `https://proceedings.mlr.press/v155/schwarting21a.html`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Deep Latent Competition: Learning to Race Using Visual Control Policies in Latent Space (C0079) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0080

Locked ratings:
- `A-C0080-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0080 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0080-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0080; retrieval_notes` and binds `https://arxiv.org/abs/2103.04909`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Latent Imagination Facilitates Zero-Shot Transfer in Autonomous Racing (C0080) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0081

Locked ratings:
- `A-C0081-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0081 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0081-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0081 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0081; retrieval_notes` and binds `https://arxiv.org/abs/2111.12083`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: VISTA 2.0: An Open, Data-driven Simulator for Multimodal Sensing and Policy Learning for Autonomous Vehicles (C0081) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0082

Locked ratings:
- `A-C0082-05` / `screening-05`: **excluded / `exclude-insufficient-detail`**. Reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- `A-C0082-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0082 metadata-only row` and binds `https://mit-racecar.github.io/`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: MIT RACECAR (C0082) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0083

Locked ratings:
- `A-C0083-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0083 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0083-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0083 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0083; retrieval_notes` and binds `https://arxiv.org/abs/2402.18558`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Unifying F1TENTH Autonomous Racing: Survey, Methods and Benchmarks (C0083) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0084

Locked ratings:
- `A-C0084-03` / `screening-03`: **excluded / `exclude-insufficient-detail`**. Reason: C0084 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- `A-C0084-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest record C0084/provisional-metadata; retrieval_notes` and binds `https://arxiv.org/abs/2506.15899`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Advancing Autonomous Racing: A Comprehensive Survey of the Roboracer (F1TENTH) Platform (C0084) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0085

Locked ratings:
- `A-C0085-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0085 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- `A-C0085-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0085` and binds `https://arxiv.org/abs/1711.07300`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Optimization-based autonomous racing of 1:43 scale RC cars (C0085) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0086

Locked ratings:
- `A-C0086-02` / `screening-02`: **excluded / `exclude-insufficient-detail`**. Reason: C0086 has no staged full text; metadata-only record cannot establish a retention condition.
- `A-C0086-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Evidence manifest row C0086; retrieval_notes` and binds `https://arxiv.org/abs/2205.15979`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: TUM autonomous motorsport: An autonomous racing software for the Indy Autonomous Challenge (C0086) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0087

Locked ratings:
- `A-C0087-04` / `screening-04`: **excluded / `exclude-insufficient-detail`**. Reason: The C0087 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- `A-C0087-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0087` and binds `https://arxiv.org/abs/2202.03807`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: Indy Autonomous Challenge - Autonomous Race Cars at the Handling Limits (C0087) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0089

Locked ratings:
- `A-C0089-01` / `screening-01`: **excluded / `exclude-insufficient-detail`**. Reason: The C0089 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- `A-C0089-06` / `screening-06`: **excluded / `exclude-insufficient-detail`**. Reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.

Triggers: `A3`.

Evidence/locator analysis: The frozen evidence packet records `abstract_only` at `evidence_packet_manifest.csv; C0089 provisional-metadata row` and binds `https://arxiv.org/abs/1806.00678`. Both locked ratings retain the same status and criterion; the differing exclusion wording is reconciled against that frozen evidence record.

Final rationale: AutoRally: An Open Platform for Aggressive Autonomous Driving (C0089) is recorded as abstract-only in the frozen evidence packet: the packet contains only a metadata retrieval record and no staged primary artifact establishing a core, supporting, or contextual retention condition.

- Recommendation: **excluded / `exclude-insufficient-detail`**
