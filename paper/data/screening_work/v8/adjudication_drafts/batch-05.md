## C0139

- Locked rating: `A-C0139-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0139 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Locked rating: `A-C0139-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest record C0139/provisional-metadata; retrieval_notes` from https://doi.org/10.1126/scirobotics.abg5810. The paired locator is `evidence_packet_manifest.csv; C0139 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0139's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0141

- Locked rating: `A-C0141-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0141 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0141-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0141 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0141; retrieval_notes` from https://doi.org/10.1145/2001576.2001631. The paired locator is `Evidence manifest record C0141/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0141's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0142

- Locked rating: `A-C0142-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0142 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0142-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0142 provisional-metadata row` from https://doi.org/10.1145/3460319.3464811. The paired locator is `evidence_packet_manifest.csv; candidate C0142; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0142's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0144

- Locked rating: `A-C0144-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0144 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0144-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0144; retrieval_notes` from https://doi.org/10.1145/3526072.3527533. The paired locator is `evidence_packet_manifest.csv; candidate C0144; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0144's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0149

- Locked rating: `A-C0149-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0149 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Locked rating: `A-C0149-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0149 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest record C0149/provisional-metadata; retrieval_notes` from https://www.frontiersin.org/journals/robotics-and-ai/articles/10.3389/frobt.2020.566037/full. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0149`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0149's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0151

- Locked rating: `A-C0151-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0151 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0151-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0151 provisional-metadata row` from https://doi.org/10.4018/ijcicg.2015070103. The paired locator is `evidence_packet_manifest.csv; C0151 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0151's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0153

- Locked rating: `A-C0153-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Locked rating: `A-C0153-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0153 metadata-only row` from https://arxiv.org/abs/1606.01540. The paired locator is `evidence_packet_manifest.csv; candidate C0153; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0153's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0155

- Locked rating: `A-C0155-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0155 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Locked rating: `A-C0155-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0155` from https://arxiv.org/abs/2206.06796. The paired locator is `evidence_packet_manifest.csv; candidate C0155; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0155's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0157

- Locked rating: `A-C0157-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0157 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0157-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0157 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0157; retrieval_notes` from https://arxiv.org/abs/2303.07243. The paired locator is `Evidence manifest record C0157/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0157's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0159

- Locked rating: `A-C0159-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0159 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0159-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0159 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0159; retrieval_notes` from https://arxiv.org/abs/2402.03947. The paired locator is `Evidence manifest record C0159/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0159's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0160

- Locked rating: `A-C0160-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0160 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Locked rating: `A-C0160-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest record C0160/provisional-metadata; retrieval_notes` from https://arxiv.org/abs/2403.04586. The paired locator is `evidence_packet_manifest.csv; candidate C0160; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0160's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0166

- Locked rating: `A-C0166-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Locked rating: `A-C0166-06` by `screening-06` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact is metadata-only: no full report or versioned documentation was supplied, so a survey claim cannot be supported.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0166 metadata-only row` from https://arxiv.org/abs/2602.24030. The paired locator is `evidence_packet_manifest.csv; candidate C0166; artifact provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0166's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0167

- Locked rating: `A-C0167-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0167 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0167-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0167 has no staged full text; metadata-only record cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0167 provisional-metadata row` from https://arxiv.org/abs/2605.13665. The paired locator is `Evidence manifest row C0167; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0167's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0169

- Locked rating: `A-C0169-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0169 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0169-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0169 provisional-metadata row` from https://www.asam.net/standards/detail/opencrg/. The paired locator is `evidence_packet_manifest.csv; C0169 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0169's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0170

- Locked rating: `A-C0170-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0170 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0170-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0170 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0170 provisional-metadata row` from https://publications.pages.asam.net/standards/ASAM_OpenDRIVE/ASAM_OpenDRIVE_Specification/v1.8.1/specification/index.html. The paired locator is `Evidence manifest record C0170/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0170's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0171

- Locked rating: `A-C0171-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0171 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0171-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0171 provisional-metadata row` from https://publications.pages.asam.net/standards/ASAM_OpenDRIVE/ASAM_OpenDRIVE_Specification/v1.9.0/specification/index.html. The paired locator is `evidence_packet_manifest.csv; C0171 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0171's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0173

- Locked rating: `A-C0173-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The staged PDF bytes match the manifest digest but the document has no readable trailer/xref and no page-level eligibility evidence could be inspected.
- Locked rating: `A-C0173-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: The staged artifact has no readable cross-reference or trailer and therefore provides no inspectable material evidence for a survey claim.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `full_text` evidence at `Staged primary-report artifact; PDF trailer/xref is unreadable` from https://proceedings.mlr.press/v155/jain21b/jain21b.pdf. The paired locator is `PDF p. 1; malformed PDF structure`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0173's staged PDF matches the manifest digest but has no readable trailer/xref, so no page-level material evidence can be inspected to support a survey claim.
- Hold: access_status is retained from the locked frozen rating/evidence binding; the adjudication draft found the artifact structurally unreadable and makes no claim that its content was inspectable. This candidate requires a corrected packet and rerating or a protocol-governed corrupt-artifact disposition before normalization.
- NEEDS_ACCOUNTABLE_AUTHOR_REVIEW
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0184

- Locked rating: `A-C0184-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0184 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Locked rating: `A-C0184-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0184` from https://fs-driverless.github.io/Formula-Student-Driverless-Simulator/v2.2.0/system-overview/. The paired locator is `evidence_packet_manifest.csv; C0184 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0184's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0185

- Locked rating: `A-C0185-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0185 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0185-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0185 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0185 provisional-metadata row` from https://github.com/AMZ-Racing/fssim. The paired locator is `Evidence manifest record C0185/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0185's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0189

- Locked rating: `A-C0189-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0189 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Locked rating: `A-C0189-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0189 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest record C0189/provisional-metadata; retrieval_notes` from https://github.com/TUMFTM/laptime-simulation. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0189`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0189's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0190

- Locked rating: `A-C0190-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0190 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Locked rating: `A-C0190-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0190` from https://proceedings.mlr.press/v164/rudin22a.html. The paired locator is `evidence_packet_manifest.csv; C0190 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0190's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0192

- Locked rating: `A-C0192-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0192 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0192-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0192 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0192; retrieval_notes` from https://carla.readthedocs.io/en/0.9.12/adv_opendrive/. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0192`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0192's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0193

- Locked rating: `A-C0193-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0193 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0193-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0193 has no staged full text; metadata-only record cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0193 provisional-metadata row` from https://docs.qgroundcontrol.com/master/en/qgc-dev-guide/file_formats/plan.html. The paired locator is `Evidence manifest row C0193; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0193's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0199

- Locked rating: `A-C0199-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0199 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0199-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0199 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0199 provisional-metadata row` from https://proceedings.mlr.press/v229/zhuang23a.html. The paired locator is `Evidence manifest record C0199/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0199's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0200

- Locked rating: `A-C0200-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0200 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0200-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0200 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0200 provisional-metadata row` from https://robonation.org/app/uploads/sites/2/2026/06/RobotX-2026_Team-Handbook-20260625.pdf. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0200`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0200's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0201

- Locked rating: `A-C0201-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0201 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0201-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0201 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0201 provisional-metadata row` from https://www.sailbot.org/wp-content/uploads/2022/05/SailBot-2022-Events.pdf. The paired locator is `Evidence manifest record C0201/provisional-metadata; retrieval_notes`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0201's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0204

- Locked rating: `A-C0204-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0204 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0204-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0204 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0204; retrieval_notes` from https://sdformat.org/tutorials/specification/pose_frame_semantics/1.7/. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0204`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0204's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0206

- Locked rating: `A-C0206-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0206 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Locked rating: `A-C0206-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0206` from https://njord.gitbook.io/2026/. The paired locator is `evidence_packet_manifest.csv; C0206 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0206's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0207

- Locked rating: `A-C0207-03` by `screening-03` is **excluded / `exclude-insufficient-detail`**; reason: C0207 has only a metadata-only packet record; no staged full text establishes a core, supporting, or contextual retention condition.
- Locked rating: `A-C0207-05` by `screening-05` is **excluded / `exclude-insufficient-detail`**; reason: Frozen packet contains only metadata and no inspected primary report or documentation establishing a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest record C0207/provisional-metadata; retrieval_notes` from https://robonation.org/app/uploads/sites/2/2021/11/VRX2022_Technical-Guide_v1.1.pdf. The paired locator is `evidence_packet_manifest.csv; C0207 metadata-only row`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0207's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0209

- Locked rating: `A-C0209-02` by `screening-02` is **excluded / `exclude-insufficient-detail`**; reason: C0209 has no staged full text; metadata-only record cannot establish a retention condition.
- Locked rating: `A-C0209-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0209 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `Evidence manifest row C0209; retrieval_notes` from https://robonation.org/app/uploads/sites/2/2023/07/VRX2023_Task-Descriptions_v1.2.pdf. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0209`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0209's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**

## C0210

- Locked rating: `A-C0210-01` by `screening-01` is **excluded / `exclude-insufficient-detail`**; reason: The C0210 frozen packet contains only a metadata record and no staged primary artifact establishing a retention condition.
- Locked rating: `A-C0210-04` by `screening-04` is **excluded / `exclude-insufficient-detail`**; reason: The C0210 packet has only the metadata-only retrieval record and no staged primary report or authoritative artifact; it cannot establish a retention condition.
- Trigger(s): `A3`
- Evidence/locator analysis: Both ratings bind `abstract_only` evidence at `evidence_packet_manifest.csv; C0210 provisional-metadata row` from https://github.com/osrf/vrx-docker. The paired locator is `Section metadata-only retrieval audit; evidence_packet_manifest.csv row C0210`; neither locked record adds inspectable eligibility evidence beyond the stated packet limitation.
- Final rationale: C0210's frozen packet contains only the provisional-metadata retrieval record and no staged primary report or authoritative documentation; it cannot establish a retention condition.
- Recommendation: **excluded / `exclude-insufficient-detail`**
